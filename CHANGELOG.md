# 변경 내역

이 문서는 Mato Blog Codex 플러그인 자체의 버전별 변경 사항을 기록합니다. 개별 사용자의 검색·생성·업로드 기록은 로컬 `HISTORY.md`에 별도로 저장됩니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)의 분류 방식을 참고하며, 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

### 변경

- 독립형 선택 OneQ 플랫폼 워크플로 추가: 사용자 소유 URL을 WordPress→Blogspot→번호 네이버 프로필 순으로 처리하되, WordPress/Blogspot을 선택하지 않으면 기존 네이버 단독 흐름만 사용
- WordPress 애플리케이션 비밀번호, Blogger OAuth 설정, Windows DPAPI 로컬 비밀값 보관, 플랫폼별 편집 프롬프트와 공개 Drive 이미지 공유 확인 절차 추가
- WordPress 공개 URL을 Blogspot HTML 링크와 Naver SmartEditor 텍스트 링크로 연결하고, Naver 링크 문구를 실제 입력·선택·링크 UI 동작으로 처리하도록 추가
- 네이버 원고 생성 입력에 `본문1:`·`인트로1:` 영역을 보존하도록 확장
- Google Blog Auto의 네이버 편집기 입력 흐름을 독립 업로더에 이식: 템플릿 부재 시 기본 편집기 폴백, `본문1:`·`인트로1:`·`본문2:` 분리 입력, URL 링크카드용 단일 줄 붙여넣기, `ㅂㅂㅂ`·`소제목`·`!!`, 표 행·열 확대/축소, 이미지 확장자·대소문자·대괄호·유니코드 파일명 폴백
- 일반 본문과 표 셀 타이핑 지연을 20ms로 고정하고, 원고 전체 붙여넣기와 DOM 직접 삽입 없이 Smart Editor UI 입력을 유지
- 임시저장 버튼과 완료 문구 확인을 최대 3회 재시도하도록 원본 동작을 이식하되, 공개 발행이 실패했을 때 임시저장으로 바꾸는 위험한 폴백은 사용하지 않음
- README를 최신 전체 사용설명서로 전면 개편해 프로필 1개/N개 생성, 블로그 URL→글쓰기 URL 정규화, 전용 Chrome 창 유지 구간, 계정별 템플릿 준비, 실제 타이핑·이미지·표 처리, 중단·재개 절차를 한 문서에 정리
- 프로필·워크플로·내부 명령·문제 해결 문서의 오래된 `기본 1개`, 창 자동 실행, 업로드 재승인과 이미지 미지원 설명을 현재 동작에 맞게 동기화
- 업로더가 네이버 `내 템플릿`의 `제목을입력해주세요1:`을 선택하고 제목·본문을 실제 타이핑하며 본문 영역, 소제목, 구분선, 표, 이미지와 URL 링크카드를 Smart Editor 동작으로 처리하도록 변경
- `발행`, `바로발행`, `자동발행`을 명시적 공개 발행 승인으로 처리해 검증 후 중간 재확인 없이 같은 작업에서 발행까지 완료하도록 워크플로 변경
- `profiles.py open`과 loopback Chrome 연결기를 추가해 `naver_N` 전용 창을 계속 열어둔 채 프로필 확인과 업로드가 같은 열린 세션에 재연결되도록 변경
- Google Blog Auto의 영구 프로필 로그인 유지 로직을 플러그인 내부 `naver_session.py`로 이식해 외부 설치본 없이 `naver_N` 세션 복원·로그인 상태 유지·글쓰기 접근 확인이 이어지도록 변경
- 사용자 소유 글 다운로드에서 `m.blog.naver.com/<ID>/<글번호>` 모바일 주소를 데스크톱 `PostView` 주소로 정규화해 처리하도록 수정
- 네이버 URL 원문·이미지 다운로드기를 플러그인 내부 `scripts/naver_url_download.py`로 이식해, 내 글 URL·검색 출처 다운로드가 `google-blog-auto` 설치본이나 소스 경로에 의존하지 않도록 변경
- 다운로드 원문을 단순 평문 대신 제목·인트로·본문·소제목·표·이미지 위치를 보존하는 `<제목>_원본_함축.txt`로 저장하도록 변경
- 원문 본문 컨테이너의 이미지를 최소 픽셀·기본 장수 제한으로 제외하지 않고 모두 수집하도록 변경
- 등록된 네이버 프로필 확인은 글쓰기 주소로 바로 이동하지 않고, 네이버 홈에서 로컬 Chrome 로그인 상태를 먼저 확인한 뒤 글쓰기 화면 접근을 검사하도록 조정
- Google Blog Auto의 생성모델·N 블로그 기능을 원본 위치와 이식 상태까지 추적하는 기능 추출 맵 추가
- 원본 TXT를 보존하면서 Mato 제목·본문 마커와 `_함축.txt`를 생성하는 글변환 Python 도구 추가
- 네이버 프로필 로그인과 임시저장을 플러그인 내부 코드만으로 실행하도록 독립화
- 원격 작업 서버 설정 없이 `naver_1`, `naver_2` 폴더를 자동 발견하고 로그인·글쓰기 권한을 확인하도록 변경
- `프로필1 "서울맛집"` 형식의 한 줄 요청을 프로필 확인 → 검색 → 생성 → 임시저장 흐름으로 연결
- 네이버 프로필 슬롯을 고정 개수로 제한하지 않고 모든 양의 정수 `naver_N`을 지원하도록 규칙과 회귀 테스트 명시
- 검색 글감·생성 원고·실행 기록의 기본 저장 위치를 한국 시간 기준 `바탕화면/YYYY-MM-DD/<실행-ID>`로 변경
- 검색 영역을 생략하면 통합검색, 생성 개수를 생략하면 원고 10개가 되도록 기본값 변경
- 통합검색에 실제 노출된 블로그 글을 최대 5개만 사용하고 출처별 제목 폴더에 재서술 글감과 이미지 위치 태그를 저장하도록 추가
- 기존 Google Blog Auto의 `run_naver_url_download()`을 직접 호출해 출처별 실제 `image_N.jpg`와 이미지 태그 포함 `_함축.txt`를 저장하는 브리지 추가
- 본인 소유·사용 허가 이미지의 EXIF 방향을 적용하고 GPS·EXIF·XMP·ICC를 제거한 뒤 같은 파일명으로 재인코딩하는 범용 이미지 정리 모듈 추가
- URL 다운로드 직후 이미지 정리를 자동 실행하고 출처 폴더에 값 비노출 처리 내역 `image-processing.json`을 저장하도록 연결
- 내 글 이미지 원본 데이터셋을 원고별로 복사·연결하고, 원고 N개마다 N회의 별도 이미지 정리와 원고별 처리 기록을 만드는 `prepare_image_datasets.py` 추가

### 예정

- 사용 권한이 확인된 이미지 또는 생성형 이미지용 확장 지점
- 추가 플랫폼과 브라우저 지원 검토

## [0.1.0] - 2026-07-22

### 추가

- GitHub 저장소에서 설치할 수 있는 Codex 플러그인과 네이버 블로그 워크플로 스킬
- Codex 내장 Browser 기반 공개 검색 수집과 Scrapling·Playwright 진단용 보조 수집기
- 블로그탭·통합검색 구분, 최대 5개 수집, 중복 제거와 URL 정규화
- Codex가 원고를 생성할 수 있는 실행 자료와 Mato `_함축.txt` 출력 규격
- 로컬 Chrome 프로필 발견·등록·확인·라운드로빈 배정 기능
- 임시저장과 자동발행을 구분한 2단계 업로드 확인
- 실행별 `run.json`, `HISTORY.md` 및 PC 전체 히스토리
- Windows 10/11과 설치된 Chrome을 대상으로 한 부트스트랩 도구
- 설치, 명령, 워크플로, 프로필, 히스토리와 문제 해결 문서
- 자연어 기본 사용법, Browser·로컬 프로필 분리와 ChatGPT 모바일 Remote 안내
- 인용 검색어와 실행 옵션 분리, 업로드 금지 표현 우선 처리
- 검증 파일 경로·SHA-256 고정과 안전한 트랜잭션 재생성
- 불확실한 업로드 결과의 수동 확인·안전 재개 상태 전이

### 보안 및 제한

- 비밀번호, 쿠키, 토큰, 로그인 세션과 계정 정보는 저장소에서 제외
- 이미지 다운로드·해시 변경·허위 EXIF 또는 메타데이터 생성 기능 제외
- CAPTCHA와 접근 제한 우회 기능 제외
- 확인 후 블로그 대상·파일 변경 차단과 성공 토스트 범위 검증
- 복합 토큰 키·Basic 인증값을 포함한 히스토리 민감정보 삭제 강화
